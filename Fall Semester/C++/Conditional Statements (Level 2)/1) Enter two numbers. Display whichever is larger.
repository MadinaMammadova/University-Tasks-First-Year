#include <iostream>
using namespace std;
int main() {
    double a,b;
    cout<<"Insert Number one = ";
    cin>>a;
    cout<<"Insert Number two = ";
    cin>>b;
if (a>=b){
    cout<<"Maximum Number = "<<a;
}   
    else
    cout<<"Maximum Number = "<<b;
    return 0;
}
