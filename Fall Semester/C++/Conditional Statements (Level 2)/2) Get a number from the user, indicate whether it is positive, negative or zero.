#include <iostream>
using namespace std;
int main() {
    double a;
    cout<<"Insert Number = ";
    cin>>a;
if (a>0){
    cout<<"Number is Positive";
}
else if(a==0)
            cout<<"Number is Zero";
      else
            cout<<"Number is Negative";
return 0;
}
