#include <iostream>
using namespace std;
int main() {
 int n;
 cout<<"Size of X = ";
 cin>>n;
 int X[n];
 cout<<"Size of Y = ";
 cin>>n;
 cout<<endl;
 int Y[n];
for(int i=0;i<n;i++) {
  cout<<"X["<<i<<"]= ";
  cin>>X[i];
}
  cout<<endl;
for(int i=0;i<n;i++) {
  cout<<"Y["<<i<<"]= ";
  cin>>Y[i];
}
for(int i=0;i<n;i++) {
 if(X[i]==Y[i]*Y[i]) {
   cout<<endl;
   cout<<"X[i] element that is the double of Y[i] = ";
   cout<<X[i];
}
}
  return 0;
}
